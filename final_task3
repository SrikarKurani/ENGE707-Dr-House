# ENGE707 Phase 1 — Task 3: Data Cleansing and Transformation
## SUPPORT2 Dataset — In-Hospital Mortality Prediction

This notebook covers Task 3 of the pipeline: cleaning up the raw SUPPORT2 data and getting it into a shape we can actually use for modelling later on. We go through it roughly in the order we worked through it ourselves — checking for obvious issues first, then dealing with missing values, outliers, and finally encoding everything for the models.

# Load the raw dataset (This notebook's stand-alone setup)
import pandas as pd
pd.set_option('display.max_rows', 100)
pd.set_option('display.width', 150)

df = pd.read_csv("data/raw/support2.csv")
print("Loaded:", df.shape)

# Before we make any changes, do a quick duplicate check directly from the raw file.
raw_dupes = df.duplicated().sum()
print("Duplicate rows in raw data:", raw_dupes)


